{
  "name": "slask-finder-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "postbuild": "cp node_modules/onnxruntime-web/dist/*.wasm dist/",
    "deploy": "scp -r dist/* dev:/srv/shared/slask-finder-web",
    "deploy-homepi": "rsync -avzOc --no-perms --no-owner --no-group --delete --exclude='*.onnx' --exclude='*.wasm' dist/ homepi:/srv/shared/slask-finder-web/ && rsync -avzOc --no-perms --no-owner --no-group --ignore-existing dist/*.onnx dist/*.wasm homepi:/srv/shared/slask-finder-web/",
    "deploy-all": "npm run build && npm run deploy",
    "deploy-pwa": "npm run build && ./deploy-local.sh",
    "lint": "biome check",
    "lint-fix": "biome check --fix",
    "lint-staged": "lint-staged",
    "preview": "vite preview",
    "prepare": "husky"
  },
  "dependencies": {
    "@matst80/slask-finder-sdk": "*",
    "framer-motion": "^12.9.2",
    "fuzzysort": "^3.1.0",
    "jotai": "^2.12.3",
    "lucide-react": "^0.503.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-markdown": "^10.1.0",
    "react-range-slider-input": "^3.2.1",
    "react-router-dom": "^7.9.4",
    "swr": "^2.3.6",
    "tailwind-scrollbar": "^4.0.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@tailwindcss/postcss": "^4.1.4",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "postcss": "^8.4.47",
    "tailwindcss": "^4.1.4",
    "typescript": "^5.8.3",
    "vite": "^7.1.11",
    "vite-bundle-analyzer": "^1.2.3",
    "vite-plugin-pwa": "^1.1.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "biome check --fix"
    ]
  }
}